extends layout

block content
  - if(!user)
    .jumbotron
      h1 欢迎来到 #{title}
      p #{title} 是一个基于 Node.js 的系统
      p
        a.btn.btn-primary.btn-lg(href='/login') 登录
        | &nbsp;
        a.btn.btn-lg(href='/signin') 立即注册
  - else
    include say